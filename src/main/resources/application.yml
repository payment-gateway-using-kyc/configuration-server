server:
  port: 8888
spring:
  cloud:
    config:
      discovery:
        enabled: true
      server:
        encrypt.enabled: false
        git:
          uri: https://github.com/payment-gateway-using-kyc/configuration-repo.git
          searchPaths:
          username:
          password: